@model IEnumerable<CPSeed.Models.Post>
@{ var link = "/tuyen-dung";}
<li class="dropdown">
    <a href="@Url.Action("Index","Pots")" class="nav-link dropdown-toggle arrow" data-toggle="dropdown">@Resource.Recruitment</a>
    <ul class="dropdown-menu">
        @foreach (var item in Model)
        {
            <li><a href="@link">@item.Title</a></li>
        }
        <li><a href="@Url.Action("Onlinefiling","Pots")">Nộp hồ sơ trực tuyến</a></li>
    </ul>
</li>


