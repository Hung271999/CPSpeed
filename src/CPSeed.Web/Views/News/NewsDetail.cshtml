@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@model IEnumerable<CPSeed.Models.NewDetail>
@using CPSeed.Util;
@Html.Action("Banner", "Home", new { image = "banner-gionglua-v1-637214382473750803.png", title = "TRUYỀN THÔNG" })
<div class="shop-box-inner">
    <div class="container">
        <div class="row">
            <div class="col-xl-3 col-lg-3 col-sm-12 col-xs-12 sidebar-shop-left ">
                <div class="product-categori ">
                    <div class="filter-sidebar-left menu-right">
                        <div class="title-left">
                            <h2 class="noo-sh-title" style="">TRUYỀN THÔNG</h2>
                        </div>
                        @Html.Action("Communicationcategory", "News")
                    </div>
                </div>
            </div>
            <div class="col-xl-9 col-lg-9 col-sm-12 col-xs-12 shop-content-right" style="padding-left:50px;">
                <h2 class="noo-sh-title" style="font-size:32px;color:#005b36;">@ViewBag.name.Title</h2>
                <p>CP Seeds cung cấp cho bà con nông dân nhiều sản phẩm hạt giống chất lượng, không chỉ kháng sâu bệnh mà còn cho năng suất và hiệu quả cao. Các sản phẩm của hạt giống CP bao gồm rau, cây trồng trên đồng ruộng và các yếu tố thực vật. Công ty có 13 sản phẩm giống ngô lai tiên tiến, chất lượng cao và 4 sản phẩm ngô ăn có chất lượng tốt nhất. Nó có 2 giống lai năng suất cao cho bông  </p>
                <br />
            </div>
        </div>
        <div class="row">
            <div class="col-lg-12">
                <div class="title-all">
                    <h2 class="noo-sh-title" style="color: #005b36; border-bottom: 2.5px solid #789A3D">@ViewBag.name.Title mới nhất</h2>
                </div>
            </div>
        </div>
        <div class="row">
            @foreach (var item in Model)
            {
            <div class="col-md-6 col-lg-6 col-xl-4">
                <div class="blog-img">
                    <img class="img-fluid" src="~/images/@item.image" alt="">
                </div>
                @{
                    var link = "/chi-tiet-tin/" + Common.str(item.Title) + "-" + item.NewDetailID;

                    var link2 = "/chi-tiet-su-kien/" + Common.str(item.Title) + "-" + item.NewDetailID;
                }  
                <div class="blog-content">
                    <div class="wp-content-news" style="margin-top:20px;">
                        @if (item.NewsID==2)
                        {
                            <a href="@link2" class="content-news-txt">@item.Title</a>
                        }
                        else
                        {
                            <a href="@link" class="content-news-txt">@item.Title</a>

                        }
                        <div class="wp-btn-detail" style="">
                            <div class="btn-detail">

                                <a style="">@ViewBag.name.Title CPSeed</a>
                            </div>
                            <p class="btn-detail-time" style="margin-left:10px">@item.CreateDate</p>
                        </div>
                    </div>

                </div>
            </div>
            }

        </div>
    </div>
    <br />
    <br />
    @Html.Action("AllNews", "News", new { id = ViewBag.id })
</div>

